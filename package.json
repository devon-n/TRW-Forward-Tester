{
  "name": "trw-forward-tester",
  "private": true,
  "devDependencies": {
    "cross-env": "^7.0.3"
  },
  "scripts": {
    "lint": "python -m pip install ruff && python -m ruff check .",
    "build": "python -m compileall app.py config.py position_sizing.py dashboard exchanges tests",
    "test": "python -m pytest"
  }
}
